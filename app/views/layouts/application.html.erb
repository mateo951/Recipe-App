<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
   <div class="container">
      <% if user_signed_in?%>
        <p>You are signed as: <%=current_user.name%></p>
        <%=button_to "Log out", 
                  destroy_user_session_path, 
                  method: :delete,
                  class:'link danger-color',
                  data: { turbo: false }
        %>
      <%end%>
    
      <% if flash.any?%>
        <% flash.each do |name, msg| -%>
          <%if name=='alert'%>
            <div class='alert'>
            <%= content_tag :div, msg, class: name %>
            </div>
          <%else%>
            <div class='notice'>
            <%= content_tag :div, msg, class: name %>
            </div>
          <% end -%>
        <% end -%>
      <%end%>
      <%= yield %>
    </div>
  </body>
</html>
