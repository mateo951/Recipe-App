  <%= turbo_stream.prepend "ingredients" do%>
    <%= render partial: "shared/ingredient", locals: {ingredient: @ingredient}%>
  <% end %>
  <%= turbo_stream.prepend "flash", partial: "shared/flash" %>